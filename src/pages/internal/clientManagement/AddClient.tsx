import React from "react";

const AddClient: React.FC = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Add New Client</h2>
      {/* Add your client creation form here */}
    </div>
  );
};

export default AddClient;